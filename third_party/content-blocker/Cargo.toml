[package]
name = "omaweb-content-blocker"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
adblock = { version = "=0.12.5", features = ["css-validation"] }
serde_json = "1.0"

[profile.release]
lto = "thin"
strip = "symbols"

