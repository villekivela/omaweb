# Use frameless application windows

Tanto windows have no native title bar or visible window controls. Tanto-owned transparent surfaces provide explicit system-move and resize regions, while platform-native application menus and keyboard commands retain minimize, close, and quit behavior. Auxiliary windows may use minimal framing when a site flow requires it, but the main and Private windows remain frameless.
